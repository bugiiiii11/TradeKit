node_modules/
dist/

# NEVER commit real env files — they contain private keys and service role credentials
.env
.env.local
.env.*.local
.env.development.local
.env.test.local
.env.production.local

# Keep the example template
!.env.example

# Frontend build artifacts (Next.js)
.next/
out/
frontend/.next/
frontend/out/
frontend/node_modules/

# Local data
data/*.db
data/*.db-journal
data/*.db-shm
data/*.db-wal
data/bt-data/
backtest-results/
backtest-results.json
emergency-snapshot.md
___temp/

# Debug / logs
debug.log

# OS / editor
.DS_Store
Thumbs.db
*.swp
.vscode/*.log

# Safety audit log (local only)
.claude/safety-audit.jsonl

# Per-user Claude Code overrides (MCP enablement, personal perms)
# Shared config lives in .claude/settings.json (committed)
.claude/settings.local.json
context-pin.md
